Aim of the project, is to research Django framework and Flask frame works and complete following tasks


Task #1
1. Create a basic Django framework based web server
2. Create Django framework server and websocket usage example
3. Create Django based MQTT server, that would print out messages sent by MQTTx

Prepare a source code and demonstration, create a short document regarding the project

Task #2
Write a small python application that would access API from https://any-api.com and would output the data from that API as JSON.

Additionally, make sure that this output is saved to the file as output.json
The extracted data from selected API, it can be either from https://any-api.com or your own selection should be have MQTT integration, that such data would be pushed to MQTT broker your preferred topic.

Prepare code samples and documentation for the code, write short summary and conclusions.